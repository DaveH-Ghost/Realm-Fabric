# Version 0.4.2 Changelog

**Purpose:** Living record of **V0.4.2** slices.

**Baseline:** Builds on **V0.4.1** (`v0.4.1`) — truncation, prompt blocks, slot settings, vision units.

**Overall V0.4.2 focus:** **Clearer spatial prompts** and **richer compound turns** — action ranges in session units; speak and emote as optional steps alongside move, look, and interact.

See also: [ROADMAP.md](../ROADMAP.md), [v0.4.1-changelog.md](v0.4.1-changelog.md).

| Slice | Theme | Status |
|-------|--------|--------|
| **0.4.2a** | Action range labels in session units | ✅ Done |
| **0.4.2b** | Speak separate from turn_action | ✅ Done |
| **0.4.2c** | Prompt input token estimate on Run turn hover | ✅ Done |
| **0.4.2d** | Emote turn action; remove confidence/emotion | ✅ Done |
| **0.4.2e** | Last response debug; release polish, tag **`v0.4.2`** | ✅ Done |

---

## 0.4.2a — Action range in session units

**Status:** ✅ **Done**

Object interaction lines in the prompt use `format_action_range_label()` when session **Units** and/or **Units per tile** are set:

- Default: `range 1` (Chebyshev tiles)
- With `ft` and 5 per tile: `range 5 ft` for range 1; `range 10 ft` for range 2
- `range 0` stays `same tile`

Threaded through `look_and_interact` slot rendering (with vision units).

---

## 0.4.2b — Speak as optional step

**Status:** ✅ **Done**

**Before:** `turn_action` was `speak | interact | none` — speak and interact were mutually exclusive.

**After:** Pipeline is **move → look → speak → turn action (interact | emote | none)**:

- `content` — optional speak dialogue (independent of `turn_action`)
- `turn_action` — `interact`, `emote`, or `none`
- Agents may speak **and** interact (or emote) in the same turn

Updated: `AgentCompoundTurn`, simulation, compound rules, output format, few-shots, `step-compound` parser.

**Breaking:** JSON with `"turn_action": "speak"` is rejected; use `"turn_action": "none"` with `content` set.

---

## 0.4.2c — Run turn token hint

**Status:** ✅ **Done**

- `estimate_prompt_tokens()` — rough chars÷4 estimate in `src/llm/token_estimate.py`
- `GET /api/prompt` returns `prompt_tokens`
- campaign-rpg-studio **Run turn ▶** shows `~N input tokens (estimate)` on hover

Not model-exact; actual billed tokens come from the LLM response after the turn runs.

---

## 0.4.2d — Emote turn action

**Status:** ✅ **Done**

**Pipeline:** move → look → speak → turn action (`interact` | `emote` | `none`).

| Field | Emote |
|-------|--------|
| `turn_action` | `"emote"` |
| `target` | Entity id or free text |
| `action_name` | Past-tense verb from LLM (e.g. `pointed`, `smiled`) |

**Result / passive phrasing:**
- Object target → `the {name}` (e.g. `You pointed at the Wooden Sign.`)
- Agent target → `{name}` (e.g. `You smiled at Goblin.`)
- Self-target → `yourself` for actor result
- Witness who **is** the target → `{actor} {verb} at you.`

No visibility or range checks in v1. Prompt rules in `look_and_interact` slot include past-tense guidance and examples.

**Removed (0.4.2):** `confidence` and `emotion` JSON fields — use `turn_action: "emote"` for non-verbal expression instead.

---

## 0.4.2e — Release polish

**Status:** ✅ **Done**

**Goal:** Tag **`v0.4.2`**; docs and tests green.

### Shipped

| In | Detail |
|----|--------|
| **Last response (debug)** | campaign-rpg-studio sidebar shows raw LLM JSON after **Run turn** (`llm_response` on `/api/turn`) |
| **This changelog** | Slice statuses ✅ for 0.4.2a–e |
| **ROADMAP / root README** | V0.4.2 summary + test counts |
| **campaign-rpg-studio README** | Emote, speak step, debug panels, breaking changes |
| **Version bump** | `pyproject.toml` **`0.4.2`**; campaign-rpg-studio **`0.4.2`** |
| **Tests** | Root **396** + campaign-rpg-studio **49** pytest green |
| **Tag** | **`git tag v0.4.2`** (manual) |

---

## Explicitly out of V0.4.2

| Item | Target |
|------|--------|
| Emote visibility / range checks | Later |
| Swappable turn schemas (`GameProfile` executors) | **V0.5+** |
| Exact token billing in UI (vs estimate) | Later |
| Multiplayer / auth | [LONG_TERM_GOALS.md](../../LONG_TERM_GOALS.md) |

---

## Release — V0.4.2 (`v0.4.2`)

**Status:** ✅ **Ready to tag**

### Ship checklist

| Step | Detail | Status |
|------|--------|--------|
| Engine | Action range units; speak + emote steps; drop confidence/emotion | ✅ |
| Example app | Token hint; last prompt + last response debug panels | ✅ |
| Tests | Root **396** + campaign-rpg-studio **49** pytest green | ✅ |
| Docs | This changelog, ROADMAP, READMEs | ✅ |
| Version | `pyproject.toml` → **`0.4.2`** | ✅ |
| Tag | **`git tag v0.4.2`** (manual) | ⬜ |

### Proof checklist

| Step | Expected |
|------|----------|
| Set Units `ft`, Units per tile `5` | Object interact lines show `range 5 ft` (etc.) |
| LLM returns speak + interact in one turn | Both steps run; speak and interact in `TurnRecord.steps` |
| LLM returns `turn_action: "emote"` | Passive / witness lines use past-tense phrasing |
| Hover **Run turn ▶** | Shows `~N input tokens (estimate)` |
| Open **Last response (debug)** after a turn | Pretty-printed LLM JSON |
| Compound JSON with `confidence` / `emotion` | Fields ignored if present in old logs; schema rejects if sent as required shape |

---

**Notes**

- **Breaking:** `"turn_action": "speak"` and optional `confidence` / `emotion` fields removed from compound schema.
- Emote has no range/visibility gate in v1 — prompt rules only.
- Last response debug is turn-scoped (no prefetch endpoint); run a turn first.

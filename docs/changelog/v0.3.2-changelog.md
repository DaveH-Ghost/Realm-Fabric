# Version 0.3.2 Changelog

**Purpose:**  
Living record of what ships in **V0.3.2** and why — **not** a pre-implementation checklist. Each section corresponds to an incremental release slice (e.g. `0.3.2a`, `0.3.2b`) so we can implement and review agilely without a full spec up front.

**Baseline:** Builds on **V0.3.1** (`v0.3.1`) — [realm-studio](../../examples/web/realm-studio) with grid UI, right-click editing, LLM **Run turn**, passive vision, and turn log. Engine remains **`0.3.0`** in root `pyproject.toml` unless a slice needs a small **`0.3.0.x`** patch with tests.

**Overall V0.3.2 focus:** Make the demo **feel more like a tabletop session** — a GM can broadcast **area events** every agent perceives, and the grid can show simple **visual identity** for agents and objects via optional `appearance` keys. Still **single-player / single session**; no multiplayer, rooms, or auth.

See also: [ROADMAP.md](../ROADMAP.md), [v0.3.1-changelog.md](v0.3.1-changelog.md), [LONG_TERM_GOALS.md](../../LONG_TERM_GOALS.md).

**Example app location:** [`examples/web/realm-studio`](../../examples/web/realm-studio)

**Planned slices (high level):**

| Slice | Theme | Status |
|-------|--------|--------|
| **0.3.2a** | Engine — area events (`emit_area_event`, passive vision, memory) | ✅ Implemented |
| **0.3.2b** | realm-studio — **Emit event** button + `POST /api/event` | ✅ Implemented |
| **0.3.2c1** | realm-studio — pannable grid viewport (white grid, black canvas) | ✅ Implemented |
| **0.3.2c2** | Engine — `appearance` image path on `Agent` / `Object` + snapshot | ✅ Implemented |
| **0.3.2d** | realm-studio — grid token images from `appearance` | ✅ Implemented |
| **0.3.2e** | Docs, tests, tag **`v0.3.2`** | ✅ Implemented |

---

## Why V0.3.2 (not multiplayer)

V0.3.1 proved the engine works through HTTP. **0.3.2** adds two missing “GM at the table” affordances:

1. **Narrator / world events** — something happens in the room that is not any agent’s action (thunder, a door slam, a smell). Every agent should **see** it in passive vision and **remember** it like a witnessed observation.
2. **Visual tokens** — optional `appearance` image paths let realm-studio show token art on the grid without the sim caring about assets.

**Multiplayer** (rooms, server-authoritative sessions, auth) is explicitly **deferred** to [LONG_TERM_GOALS.md](../../LONG_TERM_GOALS.md). V0.4’s multi-area work may reuse snapshot shapes later, but we are not building netcode in 0.3.2.

---

## Area events — design (0.3.2a–b)

Today, observable text flows only from an **agent’s** `passive_result` after a turn (`broadcast_actor_turn`). There is no **area-wide** event source.

### Target behavior

| Actor | Example |
|-------|---------|
| GM clicks **Emit event…** in realm-studio | Modal: “Thunder rumbles overhead.” |
| Engine | `Session.emit_area_event(text)` — **does not consume a turn** |
| Passive vision (all agents) | Unchanged static entity lines only (events are memory-only) |
| Memory (all agents) | Ingest as witnessed observation (environment pseudo-actor) |
| Next **Run turn** | Event appears in **Memory:** section for each agent |

### Engine API (draft)

```python
result = session.emit_area_event("Thunder rumbles overhead.")
# SessionResult(ok, message)
```

Optional CLI parity:

```text
emit-event "Thunder rumbles overhead."
```

### Snapshot (draft)

```json
{
  "recent_events": [
    { "session_turn": 3, "text": "Thunder rumbles overhead." }
  ]
}
```

Ring buffer (e.g. last **5** events) on `Area`; included in `build_area_snapshot` for UI turn log / sidebar.

### HTTP (draft)

| Method | Path | Maps to |
|--------|------|---------|
| `POST` | `/api/event` | `{ "text": "..." }` → `session.emit_area_event` |

Response: `{ ok, message, snapshot? }` — same refresh pattern as `/api/command`.

### v1 rules

- Events are **room-wide** — no range/occlusion in 0.3.2
- Events **persist** in the area log until evicted by buffer size (not single-turn flash)
- **No** turn increment on emit (same class as area edits)
- **No** streaming or scheduled events

---

## Appearance — design (0.3.2c2–d)

Visualization-only metadata. The simulation **ignores** `appearance` for movement, perception, and prompts unless we explicitly add that later.

### Engine

| Entity | Field | Example |
|--------|-------|---------|
| `Agent` | `appearance: str` | `"tokens/explorer.png"` |
| `Object` | `appearance: str` | `"tokens/ball.png"` |

- Empty string = no custom token image (realm-studio falls back to name chip)
- Value is a **client-resolved image path** (relative to realm-studio static assets, e.g. `/static/tokens/explorer.svg`)
- Exposed in `snapshot()` on every agent/object dict
- Set via `create-agent` / `edit-agent` / `create-object` / `edit-object` CLI `appearance "..."` flag

Default demo entities ship with paths under `frontend/tokens/` (hand-authored SVG placeholders).

---

## 0.3.2a — Engine area events

**Status:** ✅ **Implemented** in code.

**Goal:** `Session.emit_area_event` so all agents remember narrator/world text (memory-only; passive vision unchanged).

### Shipped

| Piece | Detail |
|-------|--------|
| **`AreaEventRecord` + ring buffer** | `Area.recent_events` (last 5) via `append_area_event` |
| **`Session.emit_area_event(text)`** | No turn increment; fans out to all agents |
| **`broadcast_area_event`** | Memory ingest via `WitnessedEvent` pseudo-actor (`__area__` / Environment) |
| **Passive vision** | Static entity lines only — no `passive_result` or area events (memory holds both) |
| **Memory formatting** | Area events render as plain `- {text}` (no actor/position suffix) |
| **CLI** | `emit-event "..."` via `run_command` and `ManualStepper` |
| **Snapshot** | `recent_events[]` with `{ session_turn, text }` |
| **Tests** | `tests/test_area_events.py` (8 tests) |

### Out (unchanged)

- Per-agent filtering (some agents deaf/blind)
- Scheduled / delayed events
- Event templates or effect hooks

---

## 0.3.2b — realm-studio emit event

**Status:** ✅ **Implemented** in code.

**Goal:** Toolbar **Emit event…** opens a modal; POST to engine; sidebar/grid refresh.

### Shipped

| Piece | Detail |
|-------|--------|
| **`POST /api/event`** | `{ "text": "..." }` → `session.emit_area_event`; returns `{ ok, message, snapshot? }` |
| **Emit event… button** | Modal + toast; refreshes from returned snapshot |
| **Recent events sidebar** | Lists `snapshot.recent_events` (newest first); not duplicated in passive vision |
| **Tests** | TestClient emit success, validation, state round-trip (17 API tests) |

### Out (unchanged)

- Event library / presets
- Right-click “emit here” (events are area-wide in v1)

---

## 0.3.2c1 — realm-studio pannable grid viewport

**Status:** ✅ **Implemented** in code.

**Goal:** Grid lives in a browser-sized black viewport; white dashed cell grid at fixed 64px cells; middle-mouse pan.

### Shipped

| Piece | Detail |
|-------|--------|
| **Viewport / world layers** | `#grid-viewport` (black, fills main area) + `#grid-world` (CSS transform pan) |
| **Layout** | Page flex column — grid panel grows with window; sidebar scrolls independently |
| **Grid style** | White background, dashed internal borders, 64px fixed cells |
| **Pan** | Middle-click drag; default cursor until panning; pan preserved on snapshot refresh |
| **Center on load** | Grid centered once in viewport when smaller than window |
| **Chips** | Restyled for contrast on white tiles |

### Out (later slices)

- Zoom (wheel / pinch)
- Coordinate labels (hidden in CSS; easy to re-enable)
- Space+drag pan fallback for trackpads

---

## 0.3.2c2 — Engine appearance field

**Status:** ✅ **Implemented** in code.

**Goal:** Optional image path on agents and objects; serialized in snapshot; editable via CLI. Simulation ignores it.

### Shipped

| Piece | Detail |
|-------|--------|
| **`Agent.appearance` / `Object.appearance`** | `str`, default `""` — client-only image path (e.g. `tokens/explorer.png`) |
| **Area edit** | `create-*` / `edit-*` accept `appearance "..."`; edit clears with `appearance ""` |
| **Snapshot** | `"appearance"` on every agent/object dict |
| **Tests** | Create/edit round-trip; no look invalidation; snapshot shape |

### Out (0.3.2d)

- realm-studio grid rendering from path
- Bundled token assets / modals

---

## 0.3.2d — realm-studio appearance on grid

**Status:** ✅ **Implemented** in code.

**Goal:** Grid shows token images from `appearance` paths; name chips when unset or image load fails.

### Shipped

| Piece | Detail |
|-------|--------|
| **Token render** | `<img>` from `appearance` path (`/static/…` resolution in `appearance.js`) |
| **Fallback** | Name chip if path empty or image 404 |
| **Bundled tokens** | `frontend/tokens/explorer.svg`, `ball.svg`, `sign.svg` on default entities |
| **Modals** | Optional **Token image path** on create/edit agent/object |
| **CLI builders** | `appearance` wired through `POST /api/command` |
| **Tests** | Static asset smoke + appearance via command (19 API tests) |

### Out

- Image picker / asset upload
- Drag-and-drop token art

---

## 0.3.2e — Release polish

**Status:** ✅ **Implemented** in code.

**Goal:** Docs and tests green; example milestone ready for tag **`v0.3.2`**.

### Shipped

| Piece | Detail |
|-------|--------|
| **Root README** | V0.3.2 status; `emit-event`, `appearance` CLI examples |
| **ROADMAP** | V0.3.2 marked complete |
| **realm-studio README** | Full V0.3.2 feature summary |
| **Tests** | Root pytest **287** tests; realm-studio **19** API tests — all green |
| **Tag** | Run `git tag v0.3.2` when ready (not automated) |

---

## Explicitly out of V0.3.2

| Item | Target |
|------|--------|
| Multiplayer / rooms / auth | [LONG_TERM_GOALS.md](../../LONG_TERM_GOALS.md) |
| Multi-area sessions | **V0.4** |
| WebSocket live sync | Later |
| Production deploy (Docker, HTTPS) | Out of scope for example |
| Full asset editor | Later |
| Agent-initiated world events | Later (GM-only in 0.3.2) |

---

## Release — V0.3.2 (`v0.3.2`)

**Status:** ✅ **Ready to tag**

### Ship checklist

| Step | Detail |
|------|--------|
| Engine | `emit_area_event` + `appearance` with pytest coverage |
| Example app | Emit event + token images visible in realm-studio |
| Tests | Root **287** + example **19** pytest green |
| Docs | This changelog, realm-studio README, ROADMAP, root README |
| Tag | **`git tag v0.3.2`** (manual) |
| Proof | GM emits event → agents' **Memory:** on next turn; grid shows token from `appearance` path |

---

**Notes**

- Prefer engine primitives in `realm_fabric` / `src/` with tests; realm-studio stays a thin HTTP/UI consumer.
- If `WitnessedEvent` needs a sibling type for area events, add it in **`0.3.0.x`** rather than only in the example app.
- Multiplayer remains aspirational until long-term design is explicit — do not slip rooms/auth into 0.3.2 “while we’re here.”

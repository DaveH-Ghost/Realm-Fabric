# Version 0.5.0 Changelog

**Purpose:** Living record of **V0.5.0** — session lorebooks + prompt block + campaign-rpg-studio Lorebooks tab.

**Baseline:** Builds on **V0.4.6** (`v0.4.6`) — custom memory modules, studio settings.

**Overall V0.5.0 focus:** **SillyTavern-format lorebooks** as optional session-level world info, injected via a per-book `lorebook` prompt slot (not in default layout).

See also: [ROADMAP.md](../ROADMAP.md), [v0.4.6-changelog.md](v0.4.6-changelog.md).

| Slice | Theme | Status |
|-------|--------|--------|
| **0.5.0a** | `src/lorebook/` models, ST import, matcher | ✅ Done |
| **0.5.0b** | Session CRUD + `lorebook` prompt slot | ✅ Done |
| **0.5.0c** | `snapshot_version: 2` save/load | ✅ Done |
| **0.5.0d** | campaign-rpg-studio Lorebooks tab + APIs | ✅ Done |
| **0.5.0e** | Prompt layout lorebook picker | ✅ Done |
| **0.5.0f** | CLI + tests + docs | ✅ Done |

---

## Lorebooks (core)

- Session holds multiple `Lorebook` objects (shared across agents)
- Import SillyTavern JSON (`entries` map with `key`, `content`, `constant`, `disable`, `selective`, `order`, …)
- Matcher: constant entries always inject; others match keywords against agent scan corpus (character, area, memory, recent GM events)
- Char budget: `lorebook_char_budget` (default 4000); honor `ignoreBudget` per entry

## Prompt block

- New slot `lorebook` — **not** in `default_prompt_blocks()`
- Requires `options: { "lorebook_id": "<id>" }` — one book per block
- Render prefix: `World info:`

## campaign-rpg-studio (initial tab)

- **Main | Lorebooks** tabs
- Lorebooks tab: load JSON, list, edit entries (enable, constant, keys, content)
- Prompt layout: add `lorebook` slot with book dropdown

## CLI

```
load-lorebook path\to\book.json
lorebooks
```

## Save format (`snapshot_version: 2`)

- New top-level `lorebooks` array + `lorebook_char_budget`
- Import of v1 saves still supported (`lorebooks` defaults to empty)

---

## Editor UX (scroll + collapse)

- Lorebooks tab fills viewport height; entry list scrolls inside the editor panel
- Loaded-books list scrolls when many books are present
- Entries default to **collapsed** (label + enable/constant only); expand per entry or use **Expand all** / **Collapse all**
- **Save changes** stays pinned at the bottom of the editor

## Add and delete entries

- **Add entry** creates a blank session entry (new `uid` / `order`) in the editor
- **Delete** removes an entry from the session copy (with confirmation)
- **Label** field edits the collapsed header text
- Status line shows unsaved vs saved entry count

## Download lorebook (ST export)

- **Download** on each book in the list; **Download JSON** in the editor footer
- `GET /api/lorebooks/{id}/download` — SillyTavern load shape (`entries` map keyed by uid string)
- Filename from original upload or `{id}.lorebook.json`
- Includes fields CampAIgn RPG Engine does not use at match time so exports work in SillyTavern and similar tools

## SillyTavern field compatibility

- Import uses **`disable` only** (not `enabled`) to match ST lorebooks
- campaign-rpg-studio **Enabled** checkbox is a readable inverse of `disable`; export writes `disable`
- Unused ST fields are stored per entry in `raw` and preserved across studio **Save** (merged by `uid`)
- Export / download fills any **missing** ST keys from defaults (see below) without overwriting values already on the entry

## ST entry defaults (`src/lorebook/st_defaults.py`)

New session entries and export normalization include typical ST fields from a standard export (e.g. `tri-system-universe.lorebook.json`):

| Field | Default |
|-------|---------|
| `probability` | `100` |
| `useProbability` | `true` |
| `depth` | `4` |
| `position` | `0` |
| `vectorized` | `false` |
| `addMemo` | `true` |
| `selective` / `selectiveLogic` | `false` / `0` |
| Recursion / match-character flags | `false` |
| `scanDepth`, `caseSensitive`, `matchWholeWords`, `useGroupScoring`, `role` | `null` |
| `sticky` / `cooldown` / `delay` | `0` |
| `triggers` | `[]` |
| `group` / `outletName` / `automationId` | `""` |
| `groupOverride` | `false` |
| `groupWeight` | `100` |
| `displayIndex` | entry `uid` when absent |

CampAIgn RPG Engine’s matcher still ignores these unless noted elsewhere (e.g. `caseSensitive` in `raw`); they exist for round-trip fidelity.

## Keyword scan sources (configurable)

- Session setting `lorebook_scan_config` — enable/disable each source used for keyword matching
- Sources: agent name, personality, description; area description; memory module text; **passive vision**; recent area events
- Matching is per **active agent** at prompt build time (constants still always inject when enabled)
- campaign-rpg-studio **Lorebooks** tab: right-hand **Keyword scan sources** panel with checkboxes and live previews for the active agent
- API: `GET/PUT /api/lorebooks/scan-config`
- Saved in session snapshot (`lorebook_scan_config`)

---

## Prompt layout fix

- Fixed slot dropdown resetting to `area_description` when choosing another slot (lorebook picker)

---

## Onboarding: create & demo lorebook

- **Create lorebook** in campaign-rpg-studio — empty book in session without uploading JSON (`POST /api/lorebooks`)
- **Load demo lorebook** — bundled three-entry example for the default demo room (`POST /api/lorebooks/load-demo`)
- Example file: `examples/lorebook/campaign-rpg-engine-demo.lorebook.json` (room, ball, sign keywords)

---

## Deferred (post–0.5.0)

- Matcher/runtime use of probability, recursion, `position` injection, vector search
- Per-agent lore visibility
- Lorebook as memory module
- campaign-rpg-studio UI for editing deferred ST fields (values are preserved/exported; editor shows core fields only)
